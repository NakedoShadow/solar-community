# Backend

Vide pour l'instant. Cible : Node.js + broker MQTT + TimescaleDB, ingestion de la
télémétrie des nœuds et équilibrage de charge (sho-balancer). Pas de code tant que
le firmware v0.2 n'a pas un format de payload stable.
